"""lisocon (InTO) — Mandanten-Config. NOCH NICHT BEFUELLT.

Wirft bewusst beim Import, damit CLIENT=lisocon nie mit leeren oder falschen
Prompts laufen kann. Befuellen in Phase 2 nach den Entscheidungen mit Reinhard
(Stimme, Sprache, Kadenz, Approver). Vorlage: clients/jolly/config.py.

Benoetigt ausserdem:
- clients/lisocon/influencers.csv (Tech-Comm/Localization-Pool)
- Notion-DB fuer lisocon (NOTION_DB_ID als Env im Railway-Service)
- eigener NOTION_TOKEN (Integration "lisocon-content-engine")
"""

raise NotImplementedError(
    "clients/lisocon/config.py ist noch nicht befuellt (Phase 2). "
    "Vorlage: clients/jolly/config.py"
)
